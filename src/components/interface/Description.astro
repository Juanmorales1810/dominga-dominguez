---
import { Image } from "astro:assets";
import { CheckIcon, Notebook, Wine } from "lucide-react";
import ImageDescription from "@/assets/description/Fotos_Oficiales-40.jpg";

const listContent = [
    "Ingredientes frescos del día.",
    "Recetas tradicionales con un toque de innovación.",
    "Ubicación privilegiada frente al mar.",
    "Atención cálida y personalizada.",
    "Terraza con vista panorámica en el tercer piso."
];
---

<section id="Description"
    class="mx-auto container flex flex-col items-start gap-20 py-16 px-2 md:py-32 md:flex-row md:items-center"
>
    <div class="w-full lg:w-[50%]">
        <h2 class="mb-8 text-3xl text-white font-extrabold">
            Vive la Experiencia Dominga Domínguez
        </h2>
        <p class="mb-10 text-zinc-300 text-xl">
            Ubicado en Caleta de Peñuelas, Coquimbo, nuestro restaurante es
            reconocido como uno de los mejores de la región gracias a nuestra
            selección de pescados frescos, mariscos y carnes premium,
            acompañados de una excelente carta de vinos y coctelería.
        </p>
        <ul class="mb-14 flex flex-col gap-4 text-muted-foreground">
            {
                listContent.map((item, index) => (
                    <li class="flex items-center gap-2 text-zinc-400 text-xl">
                        <CheckIcon className="size-7 text-primary" />
                        <!-- -->{item}
                    </li>
                ))
            }
            
        </ul>
        <div class="flex flex-col gap-4 md:flex-row md:gap-8">
            <button
            class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-10 px-4 py-2"
            >
            Explora Nuestro Menú Completo
            <Notebook className="size-4" />
            </button>
            <button
            class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-10 px-4 py-2"
            >
            Descubre Nuestra Carta de Tragos
            <Wine className="size-4" />
            </button>
        </div>
    </div>
    <div
        class="relative flex  w-full h-80 p-4 before:absolute before:inset-0 before:bg-primary/30 before:[mask-image:url(/cross-pattern.svg)] before:[mask-repeat:repeat] before:[mask-size:_32px_32px] before:saturate-0 md:h-[430px]"
    >
        <div
            class="z-1 absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-transparent to-slate-900 opacity-90"
        >
        </div>
        <div class="relative m-auto w-full md:w-[80%]">
            <Image
                src={ImageDescription}
                alt="placeholder hero"
                class="z-50 max-h-[350px] w-full rounded-md object-cover"
            />
        </div>
    </div>
</section>
