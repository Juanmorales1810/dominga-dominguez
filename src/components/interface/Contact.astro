---
import { Image } from "astro:assets";
import ReservationForm from "../reservationForm";
import ImageLocal from "@/assets/form/46-arreglado.jpg";
---

<section id="Contact" class="relative py-32 px-2 md:relative">
    <img
        src="/background.svg"
        alt=""
        fetchpriority="high"
        class="absolute -z-10 inset-0 blur-[80px] saturate-200"
    />
    <div
        class="container mx-auto bg-zinc-400/10 backdrop-blur-lg ring ring-zinc-50/20 p-4 rounded-2xl lg:grid lg:grid-cols-12"
    >
        <aside
            class="relative block h-24 lg:order-last lg:col-span-5 lg:h-full xl:col-span-6"
        >
            <Image
                alt="Local"
                src={ImageLocal}
                class="absolute inset-0 h-full w-full object-cover rounded-2xl lg:rounded-r-2xl lg:rounded-l-none"
            />
        </aside>

        <main
            class="flex items-center justify-center px-8 py-8 sm:px-12 lg:col-span-7 lg:px-16 lg:py-12 xl:col-span-6"
        >
            <div class="max-w-xl lg:max-w-3xl">
                <h3
                    class="mt-6 text-2xl font-bold text-white sm:text-3xl md:text-4xl"
                >
                    Solicita tu reserva
                </h3>

                <p class="my-4 leading-relaxed text-zinc-400">
                    Si deseas solicitar una reserva, por favor completa el
                    siguiente formulario. Nos contactaremos contigo lo más
                    pronto posible para confirmar tu petición.
                </p>

                <ReservationForm client:load />
            </div>
        </main>
    </div>
</section>
